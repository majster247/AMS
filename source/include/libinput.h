#ifndef _LIBINPUT_H
#define _LIBINPUT_H

/*
 * Minimal AMS placeholder for source ports that probe for libinput while the
 * real upstream libinput tree is staged under external/wayland-stack/libinput.
 */
struct libinput;
struct libinput_event;

#endif
